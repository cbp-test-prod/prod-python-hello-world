apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow from Branch 1
on:
  workflow_dispatch:
    inputs:
      testkey1:
        type: string
      testkey2:
        type: string
jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "My workflow from branch 1"
